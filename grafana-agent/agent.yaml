server:
  log_level: info
  http_listen_port: 12345

metrics:
  wal_directory: /tmp/agent/wal
  global:
    scrape_interval: 15s
    remote_write:
      - url: http://${REMOTE_WRITE_HOST}/api/v1/push

integrations:
  agent:
    enabled: true
