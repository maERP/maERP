<Page x:Class="maERP.Client.Features.Saless.Views.SalesListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:maERP.Client.Features.Saless.Views"
      xmlns:models="using:maERP.Client.Features.Saless.Models"
      xmlns:mvux="using:Uno.Extensions.Reactive.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header with NavigationBar -->
    <utu:NavigationBar Grid.Row="0"
                       x:Uid="SalesListPage.NavigationBar">
      <utu:NavigationBar.PrimaryCommands>
        <AppBarButton x:Uid="SalesListPage.AppBarButton.Refresh"
                      Command="{Binding Refresh, ElementName=SalessFeedView}">
          <AppBarButton.Icon>
            <FontIcon Glyph="&#xE72C;" />
          </AppBarButton.Icon>
        </AppBarButton>
        <AppBarButton x:Uid="SalesListPage.AppBarButton.New"
                      Click="NewSalesButton_Click">
          <AppBarButton.Icon>
            <FontIcon Glyph="&#xE710;" />
          </AppBarButton.Icon>
        </AppBarButton>
      </utu:NavigationBar.PrimaryCommands>
    </utu:NavigationBar>

    <!-- Search Box -->
    <Grid Grid.Row="1"
          Margin="16,12,16,8">
      <TextBox x:Name="SearchBox"
               x:Uid="SalesListPage.SearchBox"
               TextChanged="SearchBox_TextChanged"
               Padding="40,0,12,0" />
      <FontIcon Glyph="&#xE721;"
                FontSize="16"
                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="12,0,0,0"
                IsHitTestVisible="False" />
    </Grid>

    <!-- Sales Table -->
    <mvux:FeedView x:Name="SalessFeedView"
                   Grid.Row="2"
                   Margin="16,0,16,16"
                   Source="{Binding Saless}">
      <mvux:FeedView.ValueTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource OutlineVariantBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource SurfaceBrush}">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <!-- Table Header -->
              <Border Grid.Row="0"
                      Background="{ThemeResource SurfaceVariantBrush}"
                      CornerRadius="12,12,0,0"
                      BorderBrush="{ThemeResource OutlineVariantBrush}"
                      BorderThickness="0,0,0,1">
                <Grid Padding="0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="150" />
                  </Grid.ColumnDefinitions>

                  <!-- SalesId Header -->
                  <Button x:Name="HeaderSalesId"
                          Grid.Column="0"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="SalesId"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="SalesListPage.Header.SalesId"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconSalesId"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- Customer Header -->
                  <Button x:Name="HeaderCustomer"
                          Grid.Column="1"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="InvoiceAddressLastName"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="SalesListPage.Header.Customer"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconCustomer"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- Total Header -->
                  <Button x:Name="HeaderTotal"
                          Grid.Column="2"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Total"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="SalesListPage.Header.Total"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconTotal"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- Status Header -->
                  <Button x:Name="HeaderStatus"
                          Grid.Column="3"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="Status"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="SalesListPage.Header.Status"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconStatus"
                                Glyph="&#xE70D;"
                                FontSize="12"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                Visibility="Collapsed" />
                    </StackPanel>
                  </Button>

                  <!-- DateSalesed Header -->
                  <Button x:Name="HeaderDateSalesed"
                          Grid.Column="4"
                          Style="{StaticResource TextButtonStyle}"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Left"
                          Padding="16,12"
                          Tag="DateSalesed"
                          Click="SortHeader_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <TextBlock x:Uid="SalesListPage.Header.DateSalesed"
                                 Style="{StaticResource LabelLarge}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <FontIcon x:Name="SortIconDateSalesed"
                                Glyph="&#xE70E;"
                                FontSize="12"
                                Foreground="{ThemeResource PrimaryBrush}"
                                Visibility="Visible" />
                    </StackPanel>
                  </Button>
                </Grid>
              </Border>

              <!-- Table Body with ScrollViewer -->
              <ScrollViewer Grid.Row="1"
                            VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Disabled">
                <ItemsRepeater x:Name="SalesItemsRepeater"
                               ItemsSource="{Binding Data}">
                  <ItemsRepeater.Layout>
                    <StackLayout Orientation="Vertical" Spacing="0" />
                  </ItemsRepeater.Layout>
                  <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                      <Button Style="{StaticResource TextButtonStyle}"
                              HorizontalAlignment="Stretch"
                              HorizontalContentAlignment="Stretch"
                              Padding="0"
                              CornerRadius="0"
                              Click="SalesRow_Click">
                        <Grid Background="Transparent"
                              Padding="0"
                              BorderBrush="{ThemeResource OutlineVariantBrush}"
                              BorderThickness="0,0,0,1">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="150" />
                          </Grid.ColumnDefinitions>

                          <!-- SalesId -->
                          <TextBlock Grid.Column="0"
                                     Text="{Binding SalesId}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14" />

                          <!-- Customer Name -->
                          <TextBlock Grid.Column="1"
                                     Text="{Binding FullName}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14"
                                     TextTrimming="CharacterEllipsis" />

                          <!-- Total -->
                          <TextBlock Grid.Column="2"
                                     Text="{Binding Total, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14" />

                          <!-- Status -->
                          <Border Grid.Column="3"
                                  VerticalAlignment="Center"
                                  Margin="16,8"
                                  Padding="8,4"
                                  CornerRadius="4"
                                  Background="{Binding Status, Converter={StaticResource SalesStatusToBackgroundConverter}}">
                            <TextBlock Text="{Binding Status, Converter={StaticResource SalesStatusToTextConverter}}"
                                       Style="{StaticResource LabelMedium}"
                                       Foreground="{Binding Status, Converter={StaticResource SalesStatusToForegroundConverter}}" />
                          </Border>

                          <!-- DateSalesed -->
                          <TextBlock Grid.Column="4"
                                     Text="{Binding DateSalesed, Converter={StaticResource DateOnlyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                     VerticalAlignment="Center"
                                     Padding="16,14" />
                        </Grid>
                      </Button>
                    </DataTemplate>
                  </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
              </ScrollViewer>
            </Grid>
          </Border>
        </DataTemplate>
      </mvux:FeedView.ValueTemplate>

      <mvux:FeedView.ProgressTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource OutlineVariantBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource SurfaceBrush}">
            <ProgressRing IsActive="True"
                          Width="48"
                          Height="48"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="48" />
          </Border>
        </DataTemplate>
      </mvux:FeedView.ProgressTemplate>

      <mvux:FeedView.NoneTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource OutlineVariantBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource SurfaceBrush}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16"
                        Margin="48">
              <FontIcon Glyph="&#xE7BF;"
                        FontSize="48"
                        Foreground="{ThemeResource OnSurfaceVariantBrush}" />
              <TextBlock x:Uid="SalesListPage.NoSaless"
                         Style="{StaticResource BodyMedium}"
                         HorizontalAlignment="Center" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </mvux:FeedView.NoneTemplate>

      <mvux:FeedView.ErrorTemplate>
        <DataTemplate>
          <Border CornerRadius="12"
                  BorderBrush="{ThemeResource ErrorBrush}"
                  BorderThickness="1"
                  Background="{ThemeResource ErrorContainerBrush}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16"
                        Margin="48">
              <FontIcon Glyph="&#xE783;"
                        FontSize="48"
                        Foreground="{ThemeResource ErrorBrush}" />
              <TextBlock x:Uid="SalesListPage.LoadError"
                         Style="{StaticResource BodyMedium}"
                         Foreground="{ThemeResource OnErrorContainerBrush}"
                         HorizontalAlignment="Center" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </mvux:FeedView.ErrorTemplate>
    </mvux:FeedView>

    <!-- Pagination Controls -->
    <Border Grid.Row="3"
            Background="{ThemeResource SurfaceBrush}"
            BorderBrush="{ThemeResource OutlineVariantBrush}"
            BorderThickness="0,1,0,0"
            Padding="16,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Count Info (Left) -->
        <mvux:FeedView Source="{Binding Pagination}"
                       Grid.Column="0">
          <mvux:FeedView.ValueTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Data.CountInfo}"
                         Style="{StaticResource BodySmall}"
                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                         VerticalAlignment="Center" />
            </DataTemplate>
          </mvux:FeedView.ValueTemplate>
        </mvux:FeedView>

        <!-- Page Navigation (Center) -->
        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Spacing="8">
          <Button x:Name="PreviousPageButton"
                  Style="{StaticResource IconButtonStyle}"
                  x:Uid="SalesListPage.PreviousPage"
                  Click="PreviousPage_Click">
            <FontIcon Glyph="&#xE76B;"
                      FontSize="16" />
          </Button>

          <mvux:FeedView Source="{Binding Pagination}">
            <mvux:FeedView.ValueTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Data.PageInfo}"
                           Style="{StaticResource BodyMedium}"
                           VerticalAlignment="Center"
                           MinWidth="100"
                           TextAlignment="Center" />
              </DataTemplate>
            </mvux:FeedView.ValueTemplate>
          </mvux:FeedView>

          <Button x:Name="NextPageButton"
                  Style="{StaticResource IconButtonStyle}"
                  x:Uid="SalesListPage.NextPage"
                  Click="NextPage_Click">
            <FontIcon Glyph="&#xE76C;"
                      FontSize="16" />
          </Button>
        </StackPanel>

        <!-- Page Size Selection (Right) -->
        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8">
          <TextBlock x:Uid="SalesListPage.ItemsPerPage"
                     Style="{StaticResource BodySmall}"
                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                     VerticalAlignment="Center" />
          <ComboBox x:Name="PageSizeComboBox"
                    Style="{StaticResource PageSizeComboBoxStyle}"
                    SelectionChanged="PageSizeComboBox_SelectionChanged"
                    SelectedIndex="1">
            <ComboBoxItem Content="10" Tag="10" />
            <ComboBoxItem Content="25" Tag="25" />
            <ComboBoxItem Content="100" Tag="100" />
            <ComboBoxItem Content="500" Tag="500" />
            <ComboBoxItem Content="1000" Tag="1000" />
          </ComboBox>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Page>
