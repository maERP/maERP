<Page x:Class="maERP.Client.Features.Orders.Views.OrderDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:maERP.Client.Features.Orders.Views"
      xmlns:models="using:maERP.Client.Features.Orders.Models"
      xmlns:mvux="using:Uno.Extensions.Reactive.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header with back button -->
    <utu:NavigationBar Grid.Row="0" x:Uid="OrderDetailPage.NavigationBar">
      <utu:NavigationBar.MainCommand>
        <AppBarButton Icon="Back"
                      x:Uid="OrderDetailPage.AppBarButton.Back"
                      Click="BackButton_Click" />
      </utu:NavigationBar.MainCommand>
    </utu:NavigationBar>

    <!-- Order Details Content -->
    <mvux:FeedView Grid.Row="1"
                   Source="{Binding Order}">
      <mvux:FeedView.ValueTemplate>
        <DataTemplate>
          <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Padding="24">
              <!-- VisualStateManager for responsive layout -->
              <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                  <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                      <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                      <Setter Target="MainCardsGrid.MaxWidth" Value="600" />
                      <Setter Target="MainColumn1.Width" Value="*" />
                      <Setter Target="MainColumn2.Width" Value="0" />
                      <Setter Target="MainColumn3.Width" Value="0" />
                      <Setter Target="FinancialCard.(Grid.Column)" Value="0" />
                      <Setter Target="FinancialCard.(Grid.Row)" Value="0" />
                      <Setter Target="FinancialCard.Margin" Value="0,0,0,16" />
                      <Setter Target="PaymentCard.(Grid.Column)" Value="0" />
                      <Setter Target="PaymentCard.(Grid.Row)" Value="1" />
                      <Setter Target="PaymentCard.Margin" Value="0,0,0,16" />
                      <Setter Target="ShippingCard.(Grid.Column)" Value="0" />
                      <Setter Target="ShippingCard.(Grid.Row)" Value="2" />
                      <Setter Target="ShippingCard.Margin" Value="0" />
                      <Setter Target="AddressCardsGrid.MaxWidth" Value="600" />
                      <Setter Target="AddressColumn1.Width" Value="*" />
                      <Setter Target="AddressColumn2.Width" Value="0" />
                      <Setter Target="InvoiceAddressCard.(Grid.Column)" Value="0" />
                      <Setter Target="InvoiceAddressCard.(Grid.Row)" Value="0" />
                      <Setter Target="InvoiceAddressCard.Margin" Value="0,0,0,16" />
                      <Setter Target="DeliveryAddressCard.(Grid.Column)" Value="0" />
                      <Setter Target="DeliveryAddressCard.(Grid.Row)" Value="1" />
                      <Setter Target="DeliveryAddressCard.Margin" Value="0" />
                    </VisualState.Setters>
                  </VisualState>

                  <VisualState x:Name="Medium">
                    <VisualState.StateTriggers>
                      <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                      <Setter Target="MainCardsGrid.MaxWidth" Value="1200" />
                      <Setter Target="MainColumn1.Width" Value="*" />
                      <Setter Target="MainColumn2.Width" Value="*" />
                      <Setter Target="MainColumn3.Width" Value="0" />
                      <Setter Target="FinancialCard.(Grid.Column)" Value="0" />
                      <Setter Target="FinancialCard.(Grid.Row)" Value="0" />
                      <Setter Target="FinancialCard.Margin" Value="0,0,8,16" />
                      <Setter Target="PaymentCard.(Grid.Column)" Value="1" />
                      <Setter Target="PaymentCard.(Grid.Row)" Value="0" />
                      <Setter Target="PaymentCard.Margin" Value="8,0,0,16" />
                      <Setter Target="ShippingCard.(Grid.Column)" Value="0" />
                      <Setter Target="ShippingCard.(Grid.ColumnSpan)" Value="2" />
                      <Setter Target="ShippingCard.(Grid.Row)" Value="1" />
                      <Setter Target="ShippingCard.Margin" Value="0" />
                      <Setter Target="AddressCardsGrid.MaxWidth" Value="1200" />
                      <Setter Target="AddressColumn1.Width" Value="*" />
                      <Setter Target="AddressColumn2.Width" Value="*" />
                      <Setter Target="InvoiceAddressCard.(Grid.Column)" Value="0" />
                      <Setter Target="InvoiceAddressCard.(Grid.Row)" Value="0" />
                      <Setter Target="InvoiceAddressCard.Margin" Value="0,0,8,0" />
                      <Setter Target="DeliveryAddressCard.(Grid.Column)" Value="1" />
                      <Setter Target="DeliveryAddressCard.(Grid.Row)" Value="0" />
                      <Setter Target="DeliveryAddressCard.Margin" Value="8,0,0,0" />
                    </VisualState.Setters>
                  </VisualState>

                  <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                      <AdaptiveTrigger MinWindowWidth="1200" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                      <Setter Target="MainCardsGrid.MaxWidth" Value="10000" />
                      <Setter Target="MainColumn1.Width" Value="*" />
                      <Setter Target="MainColumn2.Width" Value="*" />
                      <Setter Target="MainColumn3.Width" Value="*" />
                      <Setter Target="FinancialCard.(Grid.Column)" Value="0" />
                      <Setter Target="FinancialCard.(Grid.Row)" Value="0" />
                      <Setter Target="FinancialCard.Margin" Value="0,0,8,0" />
                      <Setter Target="PaymentCard.(Grid.Column)" Value="1" />
                      <Setter Target="PaymentCard.(Grid.Row)" Value="0" />
                      <Setter Target="PaymentCard.Margin" Value="8,0,8,0" />
                      <Setter Target="ShippingCard.(Grid.Column)" Value="2" />
                      <Setter Target="ShippingCard.(Grid.ColumnSpan)" Value="1" />
                      <Setter Target="ShippingCard.(Grid.Row)" Value="0" />
                      <Setter Target="ShippingCard.Margin" Value="8,0,0,0" />
                      <Setter Target="AddressCardsGrid.MaxWidth" Value="10000" />
                      <Setter Target="AddressColumn1.Width" Value="*" />
                      <Setter Target="AddressColumn2.Width" Value="*" />
                      <Setter Target="InvoiceAddressCard.(Grid.Column)" Value="0" />
                      <Setter Target="InvoiceAddressCard.(Grid.Row)" Value="0" />
                      <Setter Target="InvoiceAddressCard.Margin" Value="0,0,8,0" />
                      <Setter Target="DeliveryAddressCard.(Grid.Column)" Value="1" />
                      <Setter Target="DeliveryAddressCard.(Grid.Row)" Value="0" />
                      <Setter Target="DeliveryAddressCard.Margin" Value="8,0,0,0" />
                    </VisualState.Setters>
                  </VisualState>
                </VisualStateGroup>
              </VisualStateManager.VisualStateGroups>

              <StackPanel Spacing="24"
                          MaxWidth="1400"
                          HorizontalAlignment="Stretch">

                <!-- Order Header Section -->
                <Border Background="{ThemeResource SurfaceBrush}"
                        CornerRadius="12"
                        Padding="24"
                        Translation="0,0,8">
                  <Border.Shadow>
                    <ThemeShadow />
                  </Border.Shadow>
                  <StackPanel HorizontalAlignment="Center"
                              Spacing="16">
                    <!-- Order Icon -->
                    <Border Width="80"
                            Height="80"
                            CornerRadius="40"
                            Background="{ThemeResource PrimaryContainerBrush}"
                            HorizontalAlignment="Center">
                      <FontIcon Glyph="&#xE7BF;"
                                FontSize="36"
                                Foreground="{ThemeResource OnPrimaryContainerBrush}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                    </Border>

                    <!-- Order Number -->
                    <StackPanel HorizontalAlignment="Center"
                                Spacing="4">
                      <TextBlock Style="{StaticResource LabelMedium}"
                                 HorizontalAlignment="Center"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                 x:Uid="OrderDetailPage.Label.OrderNumber" />
                      <TextBlock Style="{StaticResource HeadlineMedium}"
                                 HorizontalAlignment="Center"
                                 Foreground="{ThemeResource OnSurfaceBrush}">
                        <Run Text="#" /><Run Text="{Binding Data.OrderId}" />
                      </TextBlock>
                    </StackPanel>

                    <!-- Remote Order ID (if available) -->
                    <TextBlock Style="{StaticResource BodyMedium}"
                               HorizontalAlignment="Center"
                               Foreground="{ThemeResource OnSurfaceVariantBrush}"
                               Visibility="{Binding Data.RemoteOrderId, Converter={StaticResource StringToVisibilityConverter}}">
                      <Run x:Uid="OrderDetailPage.Label.RemoteOrderId" />
                      <Run Text="{Binding Data.RemoteOrderId}" />
                    </TextBlock>

                    <!-- Status Badges -->
                    <StackPanel Orientation="Horizontal"
                                Spacing="8"
                                HorizontalAlignment="Center">
                      <!-- Order Status Badge -->
                      <Border CornerRadius="16"
                              Padding="16,6"
                              Background="{Binding Data.Status, Converter={StaticResource OrderStatusToBackgroundConverter}}">
                        <TextBlock Text="{Binding Data.Status, Converter={StaticResource OrderStatusToTextConverter}}"
                                   Style="{StaticResource LabelMedium}"
                                   Foreground="{Binding Data.Status, Converter={StaticResource OrderStatusToForegroundConverter}}" />
                      </Border>

                      <!-- Payment Status Badge -->
                      <Border CornerRadius="16"
                              Padding="16,6"
                              Background="{Binding Data.PaymentStatus, Converter={StaticResource PaymentStatusToBackgroundConverter}}">
                        <TextBlock Text="{Binding Data.PaymentStatus, Converter={StaticResource PaymentStatusToTextConverter}}"
                                   Style="{StaticResource LabelMedium}"
                                   Foreground="{Binding Data.PaymentStatus, Converter={StaticResource PaymentStatusToForegroundConverter}}" />
                      </Border>
                    </StackPanel>

                    <!-- Order Date -->
                    <TextBlock Style="{StaticResource BodyMedium}"
                               HorizontalAlignment="Center"
                               Foreground="{ThemeResource OnSurfaceVariantBrush}">
                      <Run x:Uid="OrderDetailPage.Label.OrderDate" />
                      <Run Text="{Binding Data.DateOrdered, Converter={StaticResource DateTimeOffsetToStringConverter}}" />
                    </TextBlock>

                    <!-- Customer ID -->
                    <TextBlock Style="{StaticResource BodyMedium}"
                               HorizontalAlignment="Center"
                               Foreground="{ThemeResource PrimaryBrush}">
                      <Run x:Uid="OrderDetailPage.Label.CustomerId" />
                      <Run Text="{Binding Data.CustomerId}" />
                    </TextBlock>
                  </StackPanel>
                </Border>

                <!-- Financial, Payment and Shipping Cards Grid -->
                <Grid x:Name="MainCardsGrid">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="MainColumn1" Width="*" />
                    <ColumnDefinition x:Name="MainColumn2" Width="*" />
                    <ColumnDefinition x:Name="MainColumn3" Width="*" />
                  </Grid.ColumnDefinitions>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                  </Grid.RowDefinitions>

                  <!-- Financial Information Card -->
                  <Border x:Name="FinancialCard"
                          Grid.Column="0"
                          Grid.Row="0"
                          Margin="0,0,8,0"
                          Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12">
                    <StackPanel Spacing="0">
                      <!-- Card Header -->
                      <Grid Padding="16,16,16,8">
                        <StackPanel Orientation="Horizontal">
                          <FontIcon Glyph="&#xE8C7;"
                                    FontSize="20"
                                    Foreground="{ThemeResource PrimaryBrush}"
                                    Margin="0,0,12,0" />
                          <TextBlock x:Uid="OrderDetailPage.Section.FinancialInformation"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </StackPanel>
                      </Grid>

                      <!-- Financial Fields -->
                      <StackPanel Padding="16,0,16,16"
                                  Spacing="12">
                        <!-- Subtotal -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Grid.Column="0"
                                     x:Uid="OrderDetailPage.Field.Subtotal"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                          <TextBlock Grid.Column="1"
                                     Text="{Binding Data.Subtotal, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Grid>

                        <!-- Shipping Cost -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Grid.Column="0"
                                     x:Uid="OrderDetailPage.Field.ShippingCost"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                          <TextBlock Grid.Column="1"
                                     Text="{Binding Data.ShippingCost, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Grid>

                        <!-- Tax -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Grid.Column="0"
                                     x:Uid="OrderDetailPage.Field.Tax"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                          <TextBlock Grid.Column="1"
                                     Text="{Binding Data.TotalTax, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Grid>

                        <!-- Separator -->
                        <Border Height="1"
                                Background="{ThemeResource OutlineVariantBrush}"
                                Margin="0,4" />

                        <!-- Total -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Grid.Column="0"
                                     x:Uid="OrderDetailPage.Field.Total"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                          <TextBlock Grid.Column="1"
                                     Text="{Binding Data.Total, Converter={StaticResource CurrencyConverter}}"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource PrimaryBrush}" />
                        </Grid>
                      </StackPanel>
                    </StackPanel>
                  </Border>

                  <!-- Payment Information Card -->
                  <Border x:Name="PaymentCard"
                          Grid.Column="1"
                          Grid.Row="0"
                          Margin="8,0,8,0"
                          Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12">
                    <StackPanel Spacing="0">
                      <!-- Card Header -->
                      <Grid Padding="16,16,16,8">
                        <StackPanel Orientation="Horizontal">
                          <FontIcon Glyph="&#xE8C9;"
                                    FontSize="20"
                                    Foreground="{ThemeResource TertiaryBrush}"
                                    Margin="0,0,12,0" />
                          <TextBlock x:Uid="OrderDetailPage.Section.PaymentInformation"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </StackPanel>
                      </Grid>

                      <!-- Payment Fields -->
                      <StackPanel Padding="16,0,16,16"
                                  Spacing="12">
                        <!-- Payment Method -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE8C7;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.PaymentMethod"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.PaymentMethod}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       Visibility="{Binding Data.PaymentMethod, Converter={StaticResource StringToVisibilityConverter}}" />
                            <TextBlock x:Uid="OrderDetailPage.NotAvailable"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                       Visibility="{Binding Data.PaymentMethod, Converter={StaticResource StringToInverseVisibilityConverter}}" />
                          </StackPanel>
                        </Grid>

                        <!-- Payment Provider -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE912;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.PaymentProvider"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.PaymentProvider}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       Visibility="{Binding Data.PaymentProvider, Converter={StaticResource StringToVisibilityConverter}}" />
                            <TextBlock x:Uid="OrderDetailPage.NotAvailable"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                       Visibility="{Binding Data.PaymentProvider, Converter={StaticResource StringToInverseVisibilityConverter}}" />
                          </StackPanel>
                        </Grid>

                        <!-- Transaction ID -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE8EC;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.TransactionId"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.PaymentTransactionId}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding Data.PaymentTransactionId, Converter={StaticResource StringToVisibilityConverter}}" />
                            <TextBlock x:Uid="OrderDetailPage.NotAvailable"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                       Visibility="{Binding Data.PaymentTransactionId, Converter={StaticResource StringToInverseVisibilityConverter}}" />
                          </StackPanel>
                        </Grid>
                      </StackPanel>
                    </StackPanel>
                  </Border>

                  <!-- Shipping Information Card -->
                  <Border x:Name="ShippingCard"
                          Grid.Column="2"
                          Grid.Row="0"
                          Margin="8,0,0,0"
                          Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12">
                    <StackPanel Spacing="0">
                      <!-- Card Header -->
                      <Grid Padding="16,16,16,8">
                        <StackPanel Orientation="Horizontal">
                          <FontIcon Glyph="&#xE7B8;"
                                    FontSize="20"
                                    Foreground="{ThemeResource SecondaryBrush}"
                                    Margin="0,0,12,0" />
                          <TextBlock x:Uid="OrderDetailPage.Section.ShippingInformation"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </StackPanel>
                      </Grid>

                      <!-- Shipping Fields -->
                      <StackPanel Padding="16,0,16,16"
                                  Spacing="12">
                        <!-- Shipping Method -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE7B8;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.ShippingMethod"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.ShippingMethod}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       Visibility="{Binding Data.ShippingMethod, Converter={StaticResource StringToVisibilityConverter}}" />
                            <TextBlock x:Uid="OrderDetailPage.NotAvailable"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                       Visibility="{Binding Data.ShippingMethod, Converter={StaticResource StringToInverseVisibilityConverter}}" />
                          </StackPanel>
                        </Grid>

                        <!-- Shipping Provider -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE912;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.ShippingProvider"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.ShippingProvider}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}"
                                       Visibility="{Binding Data.ShippingProvider, Converter={StaticResource StringToVisibilityConverter}}" />
                            <TextBlock x:Uid="OrderDetailPage.NotAvailable"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                       Visibility="{Binding Data.ShippingProvider, Converter={StaticResource StringToInverseVisibilityConverter}}" />
                          </StackPanel>
                        </Grid>

                        <!-- Tracking ID -->
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE8EC;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.TrackingId"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.ShippingTrackingId}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource PrimaryBrush}"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding Data.ShippingTrackingId, Converter={StaticResource StringToVisibilityConverter}}" />
                            <TextBlock x:Uid="OrderDetailPage.NotAvailable"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                       Visibility="{Binding Data.ShippingTrackingId, Converter={StaticResource StringToInverseVisibilityConverter}}" />
                          </StackPanel>
                        </Grid>

                        <!-- Shipping Status -->
                        <Grid Visibility="{Binding Data.ShippingStatus, Converter={StaticResource StringToVisibilityConverter}}">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE9F5;"
                                    FontSize="16"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center" />
                          <StackPanel Grid.Column="1">
                            <TextBlock x:Uid="OrderDetailPage.Field.ShippingStatus"
                                       Style="{StaticResource LabelSmall}"
                                       Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            <TextBlock Text="{Binding Data.ShippingStatus}"
                                       Style="{StaticResource BodyMedium}"
                                       Foreground="{ThemeResource OnSurfaceBrush}" />
                          </StackPanel>
                        </Grid>
                      </StackPanel>
                    </StackPanel>
                  </Border>
                </Grid>

                <!-- Address Cards Grid -->
                <Grid x:Name="AddressCardsGrid">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="AddressColumn1" Width="*" />
                    <ColumnDefinition x:Name="AddressColumn2" Width="*" />
                  </Grid.ColumnDefinitions>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                  </Grid.RowDefinitions>

                  <!-- Invoice Address Card -->
                  <Border x:Name="InvoiceAddressCard"
                          Grid.Column="0"
                          Grid.Row="0"
                          Margin="0,0,8,0"
                          Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12">
                    <StackPanel Spacing="0">
                      <!-- Card Header -->
                      <Grid Padding="16,16,16,8">
                        <StackPanel Orientation="Horizontal">
                          <FontIcon Glyph="&#xE9D5;"
                                    FontSize="20"
                                    Foreground="{ThemeResource SecondaryBrush}"
                                    Margin="0,0,12,0" />
                          <TextBlock x:Uid="OrderDetailPage.Section.InvoiceAddress"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </StackPanel>
                      </Grid>

                      <!-- Address Content -->
                      <StackPanel Padding="16,0,16,16"
                                  Spacing="4">
                        <!-- Name -->
                        <TextBlock Style="{StaticResource BodyLarge}"
                                   Foreground="{ThemeResource OnSurfaceBrush}">
                          <Run Text="{Binding Data.InvoiceAddressFirstName}" />
                          <Run Text=" " />
                          <Run Text="{Binding Data.InvoiceAddressLastName}" />
                        </TextBlock>

                        <!-- Company (if available) -->
                        <TextBlock Text="{Binding Data.InvoiceAddressCompanyName}"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   Visibility="{Binding Data.InvoiceAddressCompanyName, Converter={StaticResource StringToVisibilityConverter}}" />

                        <!-- Street -->
                        <TextBlock Text="{Binding Data.InvoiceAddressStreet}"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceBrush}" />

                        <!-- City and ZIP -->
                        <TextBlock Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceBrush}">
                          <Run Text="{Binding Data.InvoiceAddressZip}" />
                          <Run Text=" " />
                          <Run Text="{Binding Data.InvoiceAddressCity}" />
                        </TextBlock>

                        <!-- Country -->
                        <TextBlock Text="{Binding Data.InvoiceAddressCountry}"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   Visibility="{Binding Data.InvoiceAddressCountry, Converter={StaticResource StringToVisibilityConverter}}" />

                        <!-- Phone -->
                        <Grid Margin="0,8,0,0"
                              Visibility="{Binding Data.InvoiceAddressPhone, Converter={StaticResource StringToVisibilityConverter}}">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE717;"
                                    FontSize="14"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center" />
                          <TextBlock Grid.Column="1"
                                     Text="{Binding Data.InvoiceAddressPhone}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Grid>
                      </StackPanel>
                    </StackPanel>
                  </Border>

                  <!-- Delivery Address Card -->
                  <Border x:Name="DeliveryAddressCard"
                          Grid.Column="1"
                          Grid.Row="0"
                          Margin="8,0,0,0"
                          Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12">
                    <StackPanel Spacing="0">
                      <!-- Card Header -->
                      <Grid Padding="16,16,16,8">
                        <StackPanel Orientation="Horizontal">
                          <FontIcon Glyph="&#xE7B8;"
                                    FontSize="20"
                                    Foreground="{ThemeResource SecondaryBrush}"
                                    Margin="0,0,12,0" />
                          <TextBlock x:Uid="OrderDetailPage.Section.DeliveryAddress"
                                     Style="{StaticResource TitleMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </StackPanel>
                      </Grid>

                      <!-- Address Content -->
                      <StackPanel Padding="16,0,16,16"
                                  Spacing="4">
                        <!-- Name -->
                        <TextBlock Style="{StaticResource BodyLarge}"
                                   Foreground="{ThemeResource OnSurfaceBrush}">
                          <Run Text="{Binding Data.DeliveryAddressFirstName}" />
                          <Run Text=" " />
                          <Run Text="{Binding Data.DeliveryAddressLastName}" />
                        </TextBlock>

                        <!-- Company (if available) -->
                        <TextBlock Text="{Binding Data.DeliveryAddressCompanyName}"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   Visibility="{Binding Data.DeliveryAddressCompanyName, Converter={StaticResource StringToVisibilityConverter}}" />

                        <!-- Street -->
                        <TextBlock Text="{Binding Data.DeliveryAddressStreet}"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceBrush}" />

                        <!-- City and ZIP -->
                        <TextBlock Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceBrush}">
                          <Run Text="{Binding Data.DeliveryAddressZip}" />
                          <Run Text=" " />
                          <Run Text="{Binding Data.DeliveryAddressCity}" />
                        </TextBlock>

                        <!-- Country -->
                        <TextBlock Text="{Binding Data.DeliveryAddressCountry}"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   Visibility="{Binding Data.DeliveryAddressCountry, Converter={StaticResource StringToVisibilityConverter}}" />

                        <!-- Phone -->
                        <Grid Margin="0,8,0,0"
                              Visibility="{Binding Data.DeliveryAddressPhone, Converter={StaticResource StringToVisibilityConverter}}">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                          </Grid.ColumnDefinitions>
                          <FontIcon Grid.Column="0"
                                    Glyph="&#xE717;"
                                    FontSize="14"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center" />
                          <TextBlock Grid.Column="1"
                                     Text="{Binding Data.DeliveryAddressPhone}"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceBrush}" />
                        </Grid>
                      </StackPanel>
                    </StackPanel>
                  </Border>
                </Grid>

                <!-- Order Items Section -->
                <StackPanel Spacing="12">
                  <StackPanel Orientation="Horizontal"
                              Spacing="12">
                    <FontIcon Glyph="&#xE8EF;"
                              FontSize="20"
                              Foreground="{ThemeResource SecondaryBrush}" />
                    <TextBlock x:Uid="OrderDetailPage.Section.OrderItems"
                               Style="{StaticResource TitleMedium}"
                               Foreground="{ThemeResource OnSurfaceBrush}" />
                  </StackPanel>

                  <!-- Items List -->
                  <Border Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12"
                          Padding="0">
                    <!-- Items Header -->
                    <StackPanel>
                      <Grid Padding="16,12"
                            Background="{ThemeResource SurfaceVariantBrush}"
                            CornerRadius="12,12,0,0">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" MinWidth="150" />
                          <ColumnDefinition Width="80" />
                          <ColumnDefinition Width="100" />
                          <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   x:Uid="OrderDetailPage.Header.Product"
                                   Style="{StaticResource LabelMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                        <TextBlock Grid.Column="1"
                                   x:Uid="OrderDetailPage.Header.Quantity"
                                   Style="{StaticResource LabelMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   HorizontalAlignment="Right" />
                        <TextBlock Grid.Column="2"
                                   x:Uid="OrderDetailPage.Header.UnitPrice"
                                   Style="{StaticResource LabelMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   HorizontalAlignment="Right" />
                        <TextBlock Grid.Column="3"
                                   x:Uid="OrderDetailPage.Header.ItemTotal"
                                   Style="{StaticResource LabelMedium}"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                   HorizontalAlignment="Right" />
                      </Grid>

                      <!-- Items Repeater -->
                      <ItemsRepeater ItemsSource="{Binding Data.OrderItems}">
                        <ItemsRepeater.Layout>
                          <StackLayout Orientation="Vertical" />
                        </ItemsRepeater.Layout>
                        <ItemsRepeater.ItemTemplate>
                          <DataTemplate>
                            <Border Padding="16,12"
                                    BorderBrush="{ThemeResource OutlineVariantBrush}"
                                    BorderThickness="0,1,0,0">
                              <Grid>
                                <Grid.ColumnDefinitions>
                                  <ColumnDefinition Width="*" MinWidth="150" />
                                  <ColumnDefinition Width="80" />
                                  <ColumnDefinition Width="100" />
                                  <ColumnDefinition Width="100" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0"
                                            Spacing="2">
                                  <TextBlock Text="{Binding Name}"
                                             Style="{StaticResource BodyMedium}"
                                             Foreground="{ThemeResource OnSurfaceBrush}"
                                             TextWrapping="Wrap" />
                                  <TextBlock Text="{Binding Sku}"
                                             Style="{StaticResource LabelSmall}"
                                             Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                             Visibility="{Binding Sku, Converter={StaticResource StringToVisibilityConverter}}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Quantity}"
                                           Style="{StaticResource BodyMedium}"
                                           Foreground="{ThemeResource OnSurfaceBrush}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Price, Converter={StaticResource CurrencyConverter}}"
                                           Style="{StaticResource BodyMedium}"
                                           Foreground="{ThemeResource OnSurfaceBrush}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3"
                                           Text="{Binding Total, Converter={StaticResource CurrencyConverter}}"
                                           Style="{StaticResource BodyMedium}"
                                           Foreground="{ThemeResource OnSurfaceBrush}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center" />
                              </Grid>
                            </Border>
                          </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                      </ItemsRepeater>

                      <!-- No Items Placeholder -->
                      <Border Padding="24"
                              Visibility="{Binding Data.OrderItems.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center"
                                    Spacing="8">
                          <FontIcon Glyph="&#xE8EF;"
                                    FontSize="32"
                                    Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                          <TextBlock x:Uid="OrderDetailPage.NoItems"
                                     Style="{StaticResource BodyMedium}"
                                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                     HorizontalAlignment="Center" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                  </Border>
                </StackPanel>

                <!-- Order History Section -->
                <StackPanel Spacing="12"
                            Visibility="{Binding Data.OrderHistory.Count, Converter={StaticResource ZeroToVisibilityConverter}, ConverterParameter=Invert}">
                  <StackPanel Orientation="Horizontal"
                              Spacing="12">
                    <FontIcon Glyph="&#xE81C;"
                              FontSize="20"
                              Foreground="{ThemeResource SecondaryBrush}" />
                    <TextBlock x:Uid="OrderDetailPage.Section.OrderHistory"
                               Style="{StaticResource TitleMedium}"
                               Foreground="{ThemeResource OnSurfaceBrush}" />
                  </StackPanel>

                  <!-- History Timeline -->
                  <Border Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12"
                          Padding="16">
                    <ItemsRepeater ItemsSource="{Binding Data.OrderHistory}">
                      <ItemsRepeater.Layout>
                        <StackLayout Orientation="Vertical"
                                     Spacing="0" />
                      </ItemsRepeater.Layout>
                      <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                          <Grid Margin="0,8">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="Auto" />
                              <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Timeline Dot -->
                            <StackPanel Grid.Column="0"
                                        Width="32"
                                        HorizontalAlignment="Center">
                              <Ellipse Width="12"
                                       Height="12"
                                       Fill="{ThemeResource PrimaryBrush}"
                                       HorizontalAlignment="Center" />
                              <Border Width="2"
                                      Height="24"
                                      Background="{ThemeResource OutlineVariantBrush}"
                                      HorizontalAlignment="Center" />
                            </StackPanel>

                            <!-- History Content -->
                            <StackPanel Grid.Column="1"
                                        Margin="8,0,0,0"
                                        Spacing="4">
                              <TextBlock Text="{Binding Message}"
                                         Style="{StaticResource BodyMedium}"
                                         Foreground="{ThemeResource OnSurfaceBrush}"
                                         TextWrapping="Wrap" />
                              <TextBlock Text="{Binding CreatedAt, Converter={StaticResource DateTimeOffsetToStringConverter}}"
                                         Style="{StaticResource LabelSmall}"
                                         Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                            </StackPanel>
                          </Grid>
                        </DataTemplate>
                      </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                  </Border>
                </StackPanel>

                <!-- Notes Section -->
                <StackPanel Spacing="12">
                  <StackPanel Orientation="Horizontal"
                              Spacing="12">
                    <FontIcon Glyph="&#xE70B;"
                              FontSize="20"
                              Foreground="{ThemeResource SecondaryBrush}" />
                    <TextBlock x:Uid="OrderDetailPage.Section.Notes"
                               Style="{StaticResource TitleMedium}"
                               Foreground="{ThemeResource OnSurfaceBrush}" />
                  </StackPanel>

                  <Border Background="{ThemeResource SurfaceBrush}"
                          BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="1"
                          CornerRadius="12"
                          Padding="16"
                          MinHeight="80"
                          Visibility="{Binding Data.Note, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock Text="{Binding Data.Note}"
                               Style="{StaticResource BodyMedium}"
                               Foreground="{ThemeResource OnSurfaceBrush}"
                               TextWrapping="Wrap" />
                  </Border>

                  <!-- No Notes Placeholder -->
                  <Border Background="{ThemeResource SurfaceVariantBrush}"
                          CornerRadius="12"
                          Padding="24"
                          Visibility="{Binding Data.Note, Converter={StaticResource StringToInverseVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center"
                                Spacing="8">
                      <FontIcon Glyph="&#xE70B;"
                                FontSize="32"
                                Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                      <TextBlock x:Uid="OrderDetailPage.NoNotes"
                                 Style="{StaticResource BodyMedium}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                 HorizontalAlignment="Center" />
                    </StackPanel>
                  </Border>
                </StackPanel>
              </StackPanel>
            </Grid>
          </ScrollViewer>
        </DataTemplate>
      </mvux:FeedView.ValueTemplate>

      <mvux:FeedView.ProgressTemplate>
        <DataTemplate>
          <Grid HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel Spacing="16">
              <ProgressRing IsActive="True"
                            Width="48"
                            Height="48" />
              <TextBlock x:Uid="Common.Loading"
                         Style="{StaticResource BodyMedium}"
                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                         HorizontalAlignment="Center" />
            </StackPanel>
          </Grid>
        </DataTemplate>
      </mvux:FeedView.ProgressTemplate>

      <mvux:FeedView.ErrorTemplate>
        <DataTemplate>
          <Grid HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel Spacing="16">
              <FontIcon Glyph="&#xE783;"
                        FontSize="48"
                        Foreground="{ThemeResource ErrorBrush}" />
              <TextBlock x:Uid="OrderDetailPage.LoadError"
                         Style="{StaticResource BodyMedium}"
                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                         HorizontalAlignment="Center" />
              <Button x:Uid="Common.Retry"
                      Style="{StaticResource FilledButtonStyle}"
                      HorizontalAlignment="Center" />
            </StackPanel>
          </Grid>
        </DataTemplate>
      </mvux:FeedView.ErrorTemplate>
    </mvux:FeedView>
  </Grid>
</Page>
