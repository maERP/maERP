<Page x:Class="maERP.Client.Features.Authentication.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:maERP.Client.Features.Authentication.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <StackPanel HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Width="400"
          MaxWidth="400"
          Spacing="16"
          Padding="20">

      <!-- Server URL Input -->

      <!-- Email Input -->
      <TextBlock Text="Email"
                 FontWeight="SemiBold"
                 Margin="0,8,0,4" />
      <TextBox Text="{Binding Email, Mode=TwoWay}"
               PlaceholderText="email@example.com"
               HorizontalAlignment="Stretch"
               InputScope="EmailSmtpAddress" />

      <!-- Password Input -->
      <TextBlock Text="Password"
                 FontWeight="SemiBold"
                 Margin="0,8,0,4" />
      <PasswordBox Password="{Binding Password, Mode=TwoWay}"
                   PlaceholderText="Enter your password"
                   HorizontalAlignment="Stretch" />

      <!-- Login Button -->
      <TextBlock Text="Server URL"
                 FontWeight="SemiBold"
                 Margin="0,0,0,4" />
      <TextBox Text="{Binding ServerUrl, Mode=TwoWay}"
               PlaceholderText="https://api.maerp.com or http://localhost:5000"
               HorizontalAlignment="Stretch" />
      <Button Command="{Binding Login}"
              HorizontalAlignment="Stretch"
              Margin="0,16,0,0">
        <Button.Content>
          <TextBlock Text="Login" />
        </Button.Content>
      </Button>
    </StackPanel>
  </Grid>
</Page>