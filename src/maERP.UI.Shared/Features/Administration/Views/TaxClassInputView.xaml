<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
    x:Class="maERP.UI.Shared.Features.Administration.Views.TaxClassInputView">

  <UserControl.Resources>
    <!-- Style for NumberBox with validation errors -->
    <Style TargetType="NumberBox" x:Key="ErrorNumberBoxStyle">
      <Setter Property="BorderBrush" Value="#D32F2F"/>
      <Setter Property="BorderThickness" Value="2"/>
    </Style>
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Simple Header -->
    <Grid Grid.Row="0" Margin="0,0,0,32">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <!-- Title -->
      <StackPanel Grid.Column="0" VerticalAlignment="Center">
        <TextBlock Text="{Binding PageTitle}"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Foreground="{ThemeResource ContentViewHeadingBrush}"
                   Margin="0,0,0,8"/>
        <TextBlock Text="Steuerklasse bearbeiten oder erstellen"
                   FontSize="14"
                   Foreground="{ThemeResource ContentViewSubheadingBrush}"/>
      </StackPanel>

      <!-- Action buttons -->
      <StackPanel Grid.Column="1"
                  Orientation="Horizontal"
                  Spacing="8"
                  VerticalAlignment="Center">
        <Button Content="🔄"
                Command="{Binding ClearFormCommand}"
                Visibility="{Binding IsEditMode, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                Padding="12,8"
                CornerRadius="{ThemeResource CornerRadiusS}"
                FontSize="16"
                Background="{ThemeResource SurfaceContainerBrush}"
                Foreground="{ThemeResource OnSurfaceBrush}"
                BorderThickness="0"
                ToolTipService.ToolTip="Formular leeren"/>
        <Button Content="←"
                Command="{Binding CancelCommand}"
                Padding="12,8"
                CornerRadius="{ThemeResource CornerRadiusS}"
                FontSize="16"
                Background="{ThemeResource SurfaceContainerBrush}"
                Foreground="{ThemeResource OnSurfaceBrush}"
                BorderThickness="0"
                ToolTipService.ToolTip="Zurück"/>
        <Button Content="💾"
                Command="{Binding SaveCommand}"
                IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBoolConverter}}"
                Padding="12,8"
                CornerRadius="{ThemeResource CornerRadiusS}"
                FontSize="16"
                FontWeight="Medium"
                MinWidth="36"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Background="#1C1C1E"
                Foreground="White"
                BorderThickness="0"
                ToolTipService.ToolTip="Speichern"/>
      </StackPanel>
    </Grid>

    <!-- Main content -->
    <Grid Grid.Row="1">

      <!-- Loading Indicator -->
      <StackPanel Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Spacing="16">
        <ProgressRing IsActive="True" Width="40" Height="40"/>
        <TextBlock Text="Steuerklasse wird geladen..."
                   HorizontalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
      </StackPanel>

      <!-- Error Message -->
      <Border Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
              Background="#FFF3CD"
              BorderBrush="#D6B656"
              BorderThickness="1"
              CornerRadius="{ThemeResource CornerRadiusM}"
              Padding="20"
              Margin="20"
              VerticalAlignment="Top">
        <StackPanel Spacing="8">
          <TextBlock Text="⚠️ Fehler" FontWeight="SemiBold" Foreground="#856404"/>
          <TextBlock Text="{Binding ErrorMessage}" Foreground="#856404"/>
        </StackPanel>
      </Border>

      <!-- Form Content -->
      <ScrollViewer Visibility="{Binding ShouldShowContent, Converter={StaticResource BoolToVisibilityConverter}}" Padding="24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Center">

          <!-- Basic Information Section -->
          <Border Background="{ThemeResource ControlFillColorDefaultBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="{ThemeResource CornerRadiusM}"
                  Padding="32">
            <StackPanel Spacing="24">

              <!-- Section Title -->
              <TextBlock Text="📊 Steuerklasse"
                         FontSize="18"
                         FontWeight="SemiBold"
                         Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                         HorizontalAlignment="Center"/>

              <!-- Tax Rate Field -->
              <StackPanel Spacing="8">
                <TextBlock Text="Steuersatz *"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>

                  <NumberBox Grid.Column="0"
                             Value="{Binding TaxRate, Mode=TwoWay}"
                             Minimum="0"
                             Maximum="100"
                             SmallChange="0.01"
                             SpinButtonPlacementMode="Inline"
                             IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBoolConverter}}"
                             FontSize="16"
                             Padding="12"
                             Style="{StaticResource ErrorNumberBoxStyle}"
                             Visibility="{Binding TaxRateError, Converter={StaticResource NullToVisibilityConverter}}"/>
                  <NumberBox Grid.Column="0"
                             Value="{Binding TaxRate, Mode=TwoWay}"
                             Minimum="0"
                             Maximum="100"
                             SmallChange="0.01"
                             SpinButtonPlacementMode="Inline"
                             IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBoolConverter}}"
                             FontSize="16"
                             Padding="12"
                             Visibility="{Binding TaxRateError, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}"/>
                  <TextBlock Grid.Column="1"
                             Text="%"
                             FontSize="16"
                             FontWeight="SemiBold"
                             VerticalAlignment="Center"
                             Margin="8,0,0,0"/>
                </Grid>
                <TextBlock Text="{Binding TaxRateError}"
                           Visibility="{Binding TaxRateError, Converter={StaticResource NullToVisibilityConverter}}"
                           Foreground="#D32F2F"
                           FontSize="12"
                           TextWrapping="Wrap"/>
                <TextBlock Text="Geben Sie den Steuersatz in Prozent ein (z.B. 19.00 für 19%)"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
              </StackPanel>

              <!-- Examples Section -->
              <Border Background="{ThemeResource ControlFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1"
                      CornerRadius="{ThemeResource CornerRadiusM}"
                      Padding="16">
                <StackPanel Spacing="8">
                  <TextBlock Text="📖 Beispiele"
                             FontWeight="SemiBold"
                             FontSize="13"
                             Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                  <StackPanel Spacing="4">
                    <TextBlock Text="• 19.00% = Deutscher Regelsteuersatz" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 7.00% = Deutscher ermäßigter Steuersatz" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 0.00% = Steuerfreie Waren" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                  </StackPanel>
                </StackPanel>
              </Border>

            </StackPanel>
          </Border>

        </StackPanel>
      </ScrollViewer>

    </Grid>

    <!-- Status Footer -->
    <Border Grid.Row="2"
            Background="{ThemeResource ControlFillColorDefaultBrush}"
            Padding="16,12"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0"
            CornerRadius="0,0,8,8"
            Margin="0,8,0,0"
            Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}">

      <StackPanel Orientation="Horizontal"
                  Spacing="12"
                  HorizontalAlignment="Center">
        <ProgressRing IsActive="True" Width="20" Height="20"/>
        <TextBlock Text="Steuerklasse wird gespeichert..."
                   VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
      </StackPanel>

    </Border>

  </Grid>

</UserControl>
