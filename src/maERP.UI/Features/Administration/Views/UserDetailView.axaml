<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:maERP.UI.Features.Administration.ViewModels"
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
             x:Class="maERP.UI.Features.Administration.Views.UserDetailView">

	<Grid RowDefinitions="Auto,*">

		<!-- Simple Header -->
		<Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,32">
			<!-- Title and info -->
			<StackPanel Grid.Column="0" VerticalAlignment="Center">
				<StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,8">
					<TextBlock Text="{Binding FullName}" 
							   FontSize="28" 
							   FontWeight="SemiBold"
							   Foreground="#111827"/>
					<TextBlock Text="{Binding User.Id, StringFormat=ID #{0}}" 
							   FontSize="18" 
							   FontWeight="Medium"
							   Foreground="#6B7280"
							   VerticalAlignment="Bottom"/>
					<Border Background="#E0F2FE" 
							CornerRadius="6" 
							Padding="8,4">
						<TextBlock Text="Aktiv" 
								   FontSize="12" 
								   FontWeight="SemiBold"
								   Foreground="#0369A1"/>
					</Border>
				</StackPanel>
				<TextBlock Text="{Binding User.Email}" 
						   FontSize="14" 
						   Foreground="#6B7280"/>
			</StackPanel>
			
			<!-- Action buttons -->
			<StackPanel Grid.Column="1" 
						Orientation="Horizontal" 
						Spacing="8" 
						VerticalAlignment="Center">
				<Button Content="✏️" 
						Command="{Binding EditUserCommand}"
						Padding="10"
						CornerRadius="6"
						FontSize="16"
						Background="{DynamicResource PrimaryBrush}"
						Foreground="{DynamicResource OnPrimaryBrush}"
						BorderThickness="0"
						ToolTip.Tip="Benutzer bearbeiten"/>
				<Button Content="🔄" 
						Command="{Binding RefreshCommand}"
						Padding="10"
						CornerRadius="6"
						FontSize="16"
						Background="{DynamicResource SurfaceContainerBrush}"
						Foreground="{DynamicResource OnSurfaceBrush}"
						BorderThickness="0"
						ToolTip.Tip="Aktualisieren"/>
				<Button Content="←" 
						Command="{Binding GoBackCommand}"
						Padding="10"
						CornerRadius="6"
						FontSize="16"
						Background="{DynamicResource SurfaceContainerBrush}"
						Foreground="{DynamicResource OnSurfaceBrush}"
						BorderThickness="0"
						ToolTip.Tip="Zurück zur Liste"/>
			</StackPanel>
		</Grid>

		<!-- Main content -->
		<Grid Grid.Row="1">

			<!-- Loading Indicator -->
			<StackPanel IsVisible="{Binding IsLoading}"
						VerticalAlignment="Center"
						HorizontalAlignment="Center"
						Spacing="16">
				<ProgressBar IsIndeterminate="True" Width="200"/>
				<TextBlock Text="Benutzer wird geladen..."
						   HorizontalAlignment="Center"
						   Opacity="0.7"/>
			</StackPanel>

			<!-- Error Message -->
			<Border IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
					Background="#FFF3CD"
					BorderBrush="#D6B656"
					BorderThickness="1"
					CornerRadius="8"
					Padding="20"
					Margin="20"
					VerticalAlignment="Center">
				<StackPanel Spacing="8">
					<TextBlock Text="⚠️ Fehler" FontWeight="SemiBold" Foreground="#856404"/>
					<TextBlock Text="{Binding ErrorMessage}" Foreground="#856404"/>
					<Button Content="Erneut versuchen"
							Command="{Binding RefreshCommand}"
							HorizontalAlignment="Left"/>
				</StackPanel>
			</Border>

			<!-- User Details Content -->
			<ScrollViewer IsVisible="{Binding ShouldShowContent}" Margin="24,24,24,48">
				<StackPanel Spacing="24" MaxWidth="800" HorizontalAlignment="Center">

					<!-- Basic User Information Section -->
					<Border Background="{DynamicResource ControlFillColorDefaultBrush}"
							BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
							BorderThickness="1"
							CornerRadius="8"
							Padding="32">
						<StackPanel Spacing="24">

							<!-- Section Title -->
							<!--
              <TextBlock Text="👤 Benutzerinformationen" 
                         FontSize="18" 
                         FontWeight="SemiBold"
                         HorizontalAlignment="Center"/>-->

							<!-- User Avatar and Core Info -->
							<StackPanel HorizontalAlignment="Center" Spacing="16">

								<!-- Avatar Placeholder -->
								<Border Width="80" Height="80"
										Background="{DynamicResource SystemAccentColor}"
										CornerRadius="40"
										HorizontalAlignment="Center">
									<TextBlock Text="{Binding User.Firstname, StringFormat=\{0:C0\}}"
											   FontSize="32"
											   FontWeight="Bold"
											   Foreground="{DynamicResource OnPrimaryBrush}"
											   HorizontalAlignment="Center"
											   VerticalAlignment="Center"/>
								</Border>

								<!-- Name and ID -->
								<StackPanel HorizontalAlignment="Center" Spacing="4">
									<TextBlock Text="{Binding FullName}"
											   FontSize="20"
											   FontWeight="SemiBold"
											   HorizontalAlignment="Center"/>
									<TextBlock Text="{Binding User.Email, StringFormat=E-Mail: {0}}"
											   FontSize="14"
											   Opacity="0.7"
											   HorizontalAlignment="Center"/>
									<TextBlock Text="{Binding User.Id, StringFormat=ID: {0}}"
											   FontSize="14"
											   Opacity="0.7"
											   HorizontalAlignment="Center"/>
								</StackPanel>

							</StackPanel>

							<!-- Details Grid -->
							<!--
							<Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="40" RowSpacing="16">

								<StackPanel Grid.Row="0" Grid.Column="0" Spacing="12">
									<TextBlock Text="📧 Kontaktdaten" FontWeight="SemiBold" FontSize="14"/>
									<StackPanel Spacing="8">
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="E-Mail:" FontWeight="SemiBold" Width="80"/>
											<TextBlock Text="{Binding User.Email}"/>
										</StackPanel>
									</StackPanel>
								</StackPanel>

								<StackPanel Grid.Row="0" Grid.Column="1" Spacing="12">
									<TextBlock Text="👥 Persönliche Daten" FontWeight="SemiBold" FontSize="14"/>
									<StackPanel Spacing="8">
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="Vorname:" FontWeight="SemiBold" Width="80"/>
											<TextBlock Text="{Binding User.Firstname}"/>
										</StackPanel>
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="Nachname:" FontWeight="SemiBold" Width="80"/>
											<TextBlock Text="{Binding User.Lastname}"/>
										</StackPanel>
									</StackPanel>
								</StackPanel>

							</Grid>
							-->

						</StackPanel>
					</Border>

					<!-- Contact and Actions Section -->
					<!--
					<Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
							CornerRadius="8"
							Padding="24">
						<Grid ColumnDefinitions="*,*" ColumnSpacing="40">

							<StackPanel Grid.Column="0" Spacing="12">
								<TextBlock Text="📞 Kontakt"
										   FontSize="16"
										   FontWeight="SemiBold"/>
								<StackPanel Spacing="8">
									<StackPanel Orientation="Horizontal" Spacing="8" IsVisible="{Binding HasEmail}">
										<TextBlock Text="📧 E-Mail:" FontWeight="SemiBold" Width="100"/>
										<TextBlock Text="{Binding fUser.Email}"/>
									</StackPanel>
								</StackPanel>
							</StackPanel>

							<StackPanel Grid.Column="1" Spacing="12">
								<TextBlock Text="⚡ Schnellaktionen"
										   FontSize="16"
										   FontWeight="SemiBold"/>
								<StackPanel Spacing="6">
									<Button Command="{Binding SendEmailCommand}"
											HorizontalAlignment="Left"
											IsVisible="{Binding HasEmail}"
											Padding="12,6"
											Classes="accent"
											ToolTip.Tip="{Binding User.Email, StringFormat=E-Mail an {0} senden}">
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="📧" VerticalAlignment="Center"/>
											<TextBlock Text="E-Mail senden" VerticalAlignment="Center"/>
										</StackPanel>
									</Button>
								</StackPanel>
							</StackPanel>

						</Grid>
					</Border>
					-->

					<!-- Tenant Management Section -->
					<Border Background="{DynamicResource ControlFillColorDefaultBrush}"
							BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
							BorderThickness="1"
							CornerRadius="8"
							Padding="24">
						<StackPanel Spacing="24">

							<!-- Section Title -->
							<TextBlock Text="🏢 Tenant-Verwaltung"
									   FontSize="16"
									   FontWeight="SemiBold"
									   Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

							<!-- Two-Column Layout for Management -->
							<Grid ColumnDefinitions="*,24,*" IsVisible="{Binding CanManageTenants}">
								
								<!-- Left Column: Assigned Tenants -->
								<Border Grid.Column="0"
										Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
										BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
										BorderThickness="1"
										CornerRadius="6"
										Padding="16">
									<StackPanel Spacing="12">
										
										<!-- Assigned Tenants Header -->
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="✅ Zugewiesene Tenants"
													   FontWeight="SemiBold"
													   FontSize="14"/>
											<Border Background="#E8F5E8"
													BorderBrush="#4CAF50"
													BorderThickness="1"
													CornerRadius="10"
													Padding="4,2">
												<TextBlock Text="{Binding UserTenants.Count}"
														   FontSize="10"
														   FontWeight="SemiBold"
														   Foreground="#2E7D32"/>
											</Border>
										</StackPanel>

										<!-- Loading Indicator for Assigned Tenants -->
										<StackPanel IsVisible="{Binding IsLoadingTenants}"
													HorizontalAlignment="Center"
													Orientation="Horizontal"
													Spacing="8"
													Margin="0,16">
											<ProgressBar IsIndeterminate="True" Width="100" Height="4"/>
											<TextBlock Text="Laden..."
													   FontSize="12"
													   Opacity="0.7"/>
										</StackPanel>

										<!-- Assigned Tenants List -->
										<ScrollViewer MaxHeight="300" IsVisible="{Binding !IsLoadingTenants}">
											<ItemsControl ItemsSource="{Binding UserTenants}">
												<ItemsControl.ItemTemplate>
													<DataTemplate>
														<Border Background="{DynamicResource ControlFillColorDefaultBrush}"
																BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
																BorderThickness="1"
																CornerRadius="6"
																Padding="12"
																Margin="0,0,0,6">
															<Grid ColumnDefinitions="Auto,*,Auto">
																
																<!-- Tenant Icon -->
																<Border Grid.Column="0"
																		Background="{DynamicResource SystemAccentColor}"
																		Width="32"
																		Height="32"
																		CornerRadius="16"
																		Margin="0,0,12,0">
																	<TextBlock Text="🏢"
																			   FontSize="14"
																			   HorizontalAlignment="Center"
																			   VerticalAlignment="Center"/>
																</Border>

																<!-- Tenant Info -->
																<StackPanel Grid.Column="1" 
																			VerticalAlignment="Center"
																			Spacing="2">
																	<StackPanel Orientation="Horizontal" Spacing="6">
																		<TextBlock Text="{Binding TenantName}"
																				   FontWeight="SemiBold"
																				   FontSize="12"/>
																		<Border Background="#E8F5E8"
																				BorderBrush="#4CAF50"
																				BorderThickness="1"
																				CornerRadius="8"
																				Padding="4,1"
																				IsVisible="{Binding IsDefault}">
																			<TextBlock Text="Standard"
																					   FontSize="9"
																					   FontWeight="SemiBold"
																					   Foreground="#2E7D32"/>
																		</Border>
																	</StackPanel>
																	<TextBlock Text="{Binding TenantCode}"
																			   FontSize="10"
																			   Opacity="0.6"/>
																</StackPanel>

																<!-- Management Buttons -->
																<StackPanel Grid.Column="2"
																			Orientation="Horizontal"
																			Spacing="4"
																			VerticalAlignment="Center">
																	
																	<!-- Set as Default Button -->
																	<Button Content="⭐"
																			Command="{Binding $parent[UserControl].((vm:UserDetailViewModel)DataContext).SetDefaultTenantCommand}"
																			CommandParameter="{Binding}"
																			IsVisible="{Binding !IsDefault}"
																			Width="28"
																			Height="28"
																			Padding="0"
																			CornerRadius="14"
																			Background="#FFF3E0"
																			BorderBrush="#FFB74D"
																			BorderThickness="1"
																			ToolTip.Tip="Als Standard festlegen"/>
																	
																	<!-- Remove Button -->
																	<Button Content="✕"
																			Command="{Binding $parent[UserControl].((vm:UserDetailViewModel)DataContext).RemoveTenantCommand}"
																			CommandParameter="{Binding}"
																			Width="28"
																			Height="28"
																			Padding="0"
																			CornerRadius="14"
																			Background="#FFEBEE"
																			BorderBrush="#F44336"
																			BorderThickness="1"
																			Foreground="#D32F2F"
																			ToolTip.Tip="Zuweisung entfernen"/>
																</StackPanel>

															</Grid>
														</Border>
													</DataTemplate>
												</ItemsControl.ItemTemplate>
											</ItemsControl>
										</ScrollViewer>

										<!-- No Assigned Tenants Message -->
										<Border Background="#FFF3E0"
												BorderBrush="#FFB74D"
												BorderThickness="1"
												CornerRadius="4"
												Padding="12"
												IsVisible="{Binding !IsLoadingTenants}">
											<StackPanel Spacing="4" 
														IsVisible="{Binding UserTenants.Count, Converter={x:Static ObjectConverters.Equal}, ConverterParameter=0}">
												<TextBlock Text="ℹ️ Keine Zuweisungen" 
														   FontWeight="SemiBold"
														   FontSize="12"/>
												<TextBlock Text="Diesem Benutzer sind noch keine Tenants zugewiesen"
														   FontSize="10"
														   Opacity="0.8"/>
											</StackPanel>
										</Border>

									</StackPanel>
								</Border>

								<!-- Right Column: Available Tenants -->
								<Border Grid.Column="2"
										Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
										BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
										BorderThickness="1"
										CornerRadius="6"
										Padding="16">
									<StackPanel Spacing="12">
										
										<!-- Available Tenants Header -->
										<StackPanel Orientation="Horizontal" Spacing="8">
											<TextBlock Text="➕ Verfügbare Tenants"
													   FontWeight="SemiBold"
													   FontSize="14"/>
											<Border Background="#E3F2FD"
													BorderBrush="#2196F3"
													BorderThickness="1"
													CornerRadius="10"
													Padding="4,2">
												<TextBlock Text="{Binding AvailableTenants.Count}"
														   FontSize="10"
														   FontWeight="SemiBold"
														   Foreground="#1976D2"/>
											</Border>
										</StackPanel>

										<!-- Loading Indicator for Available Tenants -->
										<StackPanel IsVisible="{Binding IsLoadingAvailableTenants}"
													HorizontalAlignment="Center"
													Orientation="Horizontal"
													Spacing="8"
													Margin="0,16">
											<ProgressBar IsIndeterminate="True" Width="100" Height="4"/>
											<TextBlock Text="Laden..."
													   FontSize="12"
													   Opacity="0.7"/>
										</StackPanel>

										<!-- Available Tenants List -->
										<ScrollViewer MaxHeight="300" IsVisible="{Binding !IsLoadingAvailableTenants}">
											<ItemsControl ItemsSource="{Binding AvailableTenants}">
												<ItemsControl.ItemTemplate>
													<DataTemplate>
														<Border Background="{DynamicResource ControlFillColorDefaultBrush}"
																BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
																BorderThickness="1"
																CornerRadius="6"
																Padding="12"
																Margin="0,0,0,6">
															<Grid ColumnDefinitions="Auto,*,Auto">
																
																<!-- Tenant Icon -->
																<Border Grid.Column="0"
																		Background="{DynamicResource SystemAccentColor}"
																		Width="32"
																		Height="32"
																		CornerRadius="16"
																		Margin="0,0,12,0">
																	<TextBlock Text="🏢"
																			   FontSize="14"
																			   HorizontalAlignment="Center"
																			   VerticalAlignment="Center"/>
																</Border>

																<!-- Tenant Info -->
																<StackPanel Grid.Column="1" 
																			VerticalAlignment="Center"
																			Spacing="2">
																	<TextBlock Text="{Binding Name}"
																			   FontWeight="SemiBold"
																			   FontSize="12"/>
																	<TextBlock Text="{Binding TenantCode}"
																			   FontSize="10"
																			   Opacity="0.6"/>
																</StackPanel>

																<!-- Add Button -->
																<Button Grid.Column="2"
																		Content="+"
																		Command="{Binding $parent[UserControl].((vm:UserDetailViewModel)DataContext).AssignTenantCommand}"
																		CommandParameter="{Binding}"
																		Width="28"
																		Height="28"
																		Padding="0"
																		CornerRadius="14"
																		Background="#E8F5E8"
																		BorderBrush="#4CAF50"
																		BorderThickness="1"
																		Foreground="#2E7D32"
																		FontWeight="Bold"
																		VerticalAlignment="Center"
																		ToolTip.Tip="Tenant zuweisen"/>

															</Grid>
														</Border>
													</DataTemplate>
												</ItemsControl.ItemTemplate>
											</ItemsControl>
										</ScrollViewer>

										<!-- No Available Tenants Message -->
										<Border Background="#E3F2FD"
												BorderBrush="#2196F3"
												BorderThickness="1"
												CornerRadius="4"
												Padding="12"
												IsVisible="{Binding !IsLoadingAvailableTenants}">
											<StackPanel Spacing="4" 
														IsVisible="{Binding AvailableTenants.Count, Converter={x:Static ObjectConverters.Equal}, ConverterParameter=0}">
												<TextBlock Text="✅ Alle zugewiesen" 
														   FontWeight="SemiBold"
														   FontSize="12"/>
												<TextBlock Text="Alle verfügbaren Tenants sind bereits zugewiesen"
														   FontSize="10"
														   Opacity="0.8"/>
											</StackPanel>
										</Border>

									</StackPanel>
								</Border>

							</Grid>

							<!-- Read-Only View for Non-Superadmins -->
							<StackPanel Spacing="16" IsVisible="{Binding !CanManageTenants}">
								
								<!-- Loading Indicator for Tenants -->
								<StackPanel IsVisible="{Binding IsLoadingTenants}"
											HorizontalAlignment="Center"
											Orientation="Horizontal"
											Spacing="8"
											Margin="0,16">
									<ProgressBar IsIndeterminate="True" Width="120" Height="4"/>
									<TextBlock Text="Lade Tenant-Zuweisungen..."
											   FontSize="12"
											   Opacity="0.7"/>
								</StackPanel>

								<!-- Tenant List (Read-Only) -->
								<ItemsControl ItemsSource="{Binding UserTenants}"
											  IsVisible="{Binding !IsLoadingTenants}">
									<ItemsControl.ItemTemplate>
										<DataTemplate>
											<Border Background="{DynamicResource SystemControlBackgroundAltMediumBrush}"
													BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
													BorderThickness="1"
													CornerRadius="6"
													Padding="16"
													Margin="0,0,0,8">
												<Grid ColumnDefinitions="Auto,*,Auto">
													
													<!-- Tenant Icon -->
													<Border Grid.Column="0"
															Background="{DynamicResource SystemAccentColor}"
															Width="40"
															Height="40"
															CornerRadius="20"
															Margin="0,0,16,0">
														<TextBlock Text="🏢"
																   FontSize="20"
																   HorizontalAlignment="Center"
																   VerticalAlignment="Center"/>
													</Border>

													<!-- Tenant Info -->
													<StackPanel Grid.Column="1" 
																VerticalAlignment="Center"
																Spacing="4">
														<StackPanel Orientation="Horizontal" Spacing="8">
															<TextBlock Text="{Binding TenantName}"
																	   FontWeight="SemiBold"
																	   FontSize="14"/>
															<Border Background="#E8F5E8"
																	BorderBrush="#4CAF50"
																	BorderThickness="1"
																	CornerRadius="10"
																	Padding="6,2"
																	IsVisible="{Binding IsDefault}">
																<TextBlock Text="Standard"
																		   FontSize="10"
																		   FontWeight="SemiBold"
																		   Foreground="#2E7D32"/>
															</Border>
														</StackPanel>
														<TextBlock Text="{Binding TenantCode, StringFormat=Code: {0}}"
																   FontSize="12"
																   Opacity="0.7"/>
													</StackPanel>

													<!-- Status Icon -->
													<Border Grid.Column="2"
															Background="#E3F2FD"
															CornerRadius="12"
															Padding="8,4"
															VerticalAlignment="Center">
														<TextBlock Text="✓"
																   FontSize="12"
																   FontWeight="Bold"
																   Foreground="#1976D2"/>
													</Border>

												</Grid>
											</Border>
										</DataTemplate>
									</ItemsControl.ItemTemplate>
								</ItemsControl>

								<!-- No Tenants Message -->
								<Border Background="#FFF3E0"
										BorderBrush="#FFB74D"
										BorderThickness="1"
										CornerRadius="4"
										Padding="16"
										IsVisible="{Binding !IsLoadingTenants}">
									<StackPanel Spacing="8" 
												IsVisible="{Binding UserTenants.Count, Converter={x:Static ObjectConverters.Equal}, ConverterParameter=0}">
										<TextBlock Text="ℹ️ Keine Tenant-Zuweisungen" FontWeight="SemiBold"/>
										<TextBlock Text="Diesem Benutzer sind noch keine Tenants zugewiesen"
												   FontSize="12"
												   Opacity="0.8"/>
										<TextBlock Text="Wenden Sie sich an einen Superadmin, um Tenant-Zuweisungen zu verwalten"
												   FontSize="12"
												   Opacity="0.6"/>
									</StackPanel>
								</Border>

							</StackPanel>

						</StackPanel>
					</Border>

					<!-- Security Information Section -->
					<Border Background="{DynamicResource ControlFillColorDefaultBrush}"
							BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
							BorderThickness="1"
							CornerRadius="8"
							Padding="24">
						<StackPanel Spacing="16">

							<!-- Section Title -->
							<TextBlock Text="🔐 Sicherheitsinformationen"
									   FontSize="16"
									   FontWeight="SemiBold"
									   Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

							<!-- Security Info -->
							<Border Background="#FFF3E0"
									BorderBrush="#FFB74D"
									BorderThickness="1"
									CornerRadius="4"
									Padding="16">
								<StackPanel Spacing="8">
									<TextBlock Text="🔒 Passwort-Status" FontWeight="SemiBold"/>
									<TextBlock Text="Das Passwort ist aus Sicherheitsgründen nicht sichtbar"
											   FontSize="12"
											   Opacity="0.8"/>
									<TextBlock Text="Zur Passwort-Änderung nutzen Sie die Bearbeiten-Funktion"
											   FontSize="12"
											   Opacity="0.6"/>
								</StackPanel>
							</Border>

						</StackPanel>
					</Border>

				</StackPanel>
			</ScrollViewer>

		</Grid>

	</Grid>

</UserControl>